-- SoulMatch Migration
-- version: 7.0

-- This script adds the 'image_url' column to the 'places' table
-- to support storing a main image URL for venues/places.
-- It is designed to be run on an existing database without data loss.

ALTER TABLE public.places
ADD COLUMN IF NOT EXISTS image_url text;

-- End of migration script.