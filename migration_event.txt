-- SoulMatch Event Migration: Add Image URL
-- version: 6.1

-- This script adds the 'image_url' column to the 'events' table
-- to support storing URLs for AI-generated event images.
-- It is designed to be run on an existing database without data loss.

ALTER TABLE public.events
ADD COLUMN IF NOT EXISTS image_url text;

-- End of migration script.
